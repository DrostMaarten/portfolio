# Kopieer dit bestand naar .env en vul je eigen waarden in.
# .env hoort NOOIT in versiebeheer of in een verstuurde zip te zitten.

# --- SMTP (contactformulier) ---
SMTP_HOST=smtp.mailersend.net
SMTP_PORT=587
SMTP_USER=jouw-smtp-gebruikersnaam
SMTP_PASS=jouw-smtp-wachtwoord
MAIL_FROM=no-reply@jouwdomein.nl
MAIL_TO=drostmaarten05@gmail.com

# --- Database (alleen nodig als je includes/db.php gebruikt) ---
DB_HOST=localhost
DB_NAME=portfolio
DB_USER=root
DB_PASS=
